<%= simple_form_for(@reserva) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :salida %>
    <%= f.input :hotel %>
    <%= f.input :monto %>
    <%= f.association :wholesaler %>
    <%= f.association :retail %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
