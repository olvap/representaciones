<%= simple_form_for([@operator, @movement], html: { class: 'form-inline' }) do |f| %>
  <%= f.error_notification %>

  <div class="form-groups">
    <%= f.input :monto %>
    <%= f.input :type, as: :hidden %>

    <%= f.button :submit, 'Agregar' %>
  </div>
<% end %>
